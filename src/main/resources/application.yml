spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/toy_project
    username: root
    password: maria
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect
        show_sql: true

server:
  port: 9003

logging:
  level:
    com.spring.toyproject: DEBUG
    # SQL 로그
    org.hibernate.SQL: DEBUG
    # 요청 들어오는 로그레벨
    org.springframework.web: DEBUG
    # SQL 파라미터 바인딩 확인
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE


# jwt setting
jwt:
  # secret key(실제 운여에서는 환경변수로 관리)
  secret: YS76vg7c0qHr3CeavWh0XTC8k2xPuqHtmlw9k5fUhX4=
  # 만료시간 (밀리초) - 24시간
  expiration: 86400000


